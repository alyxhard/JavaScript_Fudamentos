function calcularPotencia(base, expoente) {
    return Math.pow(base, expoente);
}

// Exemplo de uso:
var resultado = calcularPotencia(2, 3); // Isso retorna 8, pois 2 elevado a 3 é igual a 8
console.log(resultado);
